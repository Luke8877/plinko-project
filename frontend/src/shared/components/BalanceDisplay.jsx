/**
 * BalanceDisplay Component
 * ---------------------------------------------------------
 * Placeholder component for a compact balance UI element.
 *
 * Potential future uses:
 * - Global balance shown in a top navigation bar
 * - Live wallet indicator separate from Dashboard card
 *
 * Currently unused in MVP — exists for layout flexibility
 * if UI requires a smaller balance element later.
 */
