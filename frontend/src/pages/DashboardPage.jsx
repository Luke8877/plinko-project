/**
 * Dashboard Page
 * --------------------------------------------------
 * Authenticated landing page after a successful login.
 * Will display user balance, recent activity, and key stats.
 *
 * Note:
 * Currently a placeholder until data-fetching and UI
 * components are connected in the next milestone phase.
 */

export default function DashboardPage() {
  return (
    <div className="text-white p-10">
      {/* Placeholder content — replaced in Dashboard feature branch */}
      Dashboard (placeholder)
    </div>
  );
}
